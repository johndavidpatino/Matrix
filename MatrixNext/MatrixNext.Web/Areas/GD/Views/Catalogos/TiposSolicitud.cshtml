@model System.Collections.Generic.List<MatrixNext.Web.Models.ViewModels.GD.TipoSolicitudViewModel>
@{
    ViewData["Title"] = "Tipos de Solicitud";
}

<div class="container mt-4">
    <h2>Tipos de Solicitud</h2>

    <button class="btn btn-primary mb-3" id="btnCreateTipo">
        <i class="fas fa-plus"></i> Nuevo Tipo
    </button>

    <table class="table table-striped" id="tblTipos">
        <thead>
            <tr>
                <th>ID</th>
                <th>Nombre</th>
                <th>Descripción</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model ?? new System.Collections.Generic.List<MatrixNext.Web.Models.ViewModels.GD.TipoSolicitudViewModel>())
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.Nombre</td>
                    <td>@item.Descripcion</td>
                    <td>
                        <button class="btn btn-sm btn-warning btnEdit" data-id="@item.Id">
                            <i class="fas fa-edit"></i>
                        </button>
                        <form method="post" class="d-inline frmDelete" data-id="@item.Id">
                            @Html.AntiForgeryToken()
                            <button type="submit" class="btn btn-sm btn-danger">
                                <i class="fas fa-trash"></i>
                            </button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@section Scripts {
    <script>
        $(function() {
            $('#btnCreateTipo').click(function() {
                $.get('@Url.Action("CreateTipo")', function(html) {
                    $('#ajaxModal').html(html).modal('show');
                });
            });

            $(document).on('click', '.btnEdit', function() {
                var id = $(this).data('id');
                $.get('@Url.Action("UpdateTipo")/' + id, function(html) {
                    $('#ajaxModal').html(html).modal('show');
                });
            });

            $(document).on('submit', '.frmDelete', function(e) {
                e.preventDefault();
                if (confirm('¿Está seguro?')) {
                    var id = $(this).data('id');
                    $.ajax({
                        type: 'POST',
                        url: '@Url.Action("DeleteTipo")',
                        data: { id: id },
                        headers: { 'X-Requested-With': 'XMLHttpRequest' },
                        success: function(result) {
                            if (result.success) {
                                location.reload();
                            }
                        },
                        error: function() { alert('Error al eliminar'); }
                    });
                }
            });
        });
    </script>
}
