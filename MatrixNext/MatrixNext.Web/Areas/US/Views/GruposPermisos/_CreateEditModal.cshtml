@model MatrixNext.Data.Modules.US.GruposPermisos.Models.GrupoPermisoDto

@{
    var isNew = Model.Id == 0;
    var titulo = isNew ? "Crear Grupo de Permisos" : "Editar Grupo de Permisos";
    var action = isNew ? "Create" : "Edit";
    var readonlyAttr = isNew ? "" : "readonly";
}

<div class="modal fade" id="modalCreateEdit" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <form id="formGrupoPermiso" asp-area="US" asp-controller="GruposPermisos" asp-action="@action" method="post">
                @Html.AntiForgeryToken()
                <div class="modal-header">
                    <h5 class="modal-title">@titulo</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Cerrar"></button>
                </div>
                <div class="modal-body">
                    <div class="mb-3">
                        <label asp-for="Id" class="form-label">Id</label>
                        @if (isNew)
                        {
                            <input asp-for="Id" class="form-control" />
                        }
                        else
                        {
                            <input asp-for="Id" class="form-control" readonly />
                        }
                        <span asp-validation-for="Id" class="text-danger"></span>
                    </div>
                    <div class="mb-3">
                        <label asp-for="GrupoPermisos" class="form-label">Nombre del Grupo</label>
                        <input asp-for="GrupoPermisos" class="form-control" required />
                        <span asp-validation-for="GrupoPermisos" class="text-danger"></span>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                    <button type="submit" class="btn btn-primary">@(isNew ? "Crear" : "Guardar")</button>
                </div>
            </form>
        </div>
    </div>
</div>

<script>
    document.getElementById('formGrupoPermiso')?.addEventListener('submit', function(e) {
        e.preventDefault();
        var form = this;
        var formData = new FormData(form);
        
        fetch(form.action, {
            method: 'POST',
            body: formData
        })
        .then(r => r.json())
        .then(data => {
            if (data.success) {
                // Cerrar modal
                var modal = bootstrap.Modal.getInstance(document.getElementById('modalCreateEdit'));
                modal.hide();
                
                // Mostrar mensaje de éxito
                alert(data.message);
                
                // Recargar la lista
                location.reload();
            } else {
                alert(data.message || 'Error al guardar');
            }
        })
        .catch(err => {
            console.error(err);
            alert('Error al procesar la solicitud');
        });
    });
</script>
