@using MatrixNext.Web.ViewModels
@model PaginationResultVM<MatrixNext.Web.Models.CORE.WorkFlow>

@{
    ViewBag.Title = "WorkFlow";
}

<h2>WorkFlow</h2>

<p class="mb-3">
    <a href="#" class="btn btn-primary"
       data-modal-url="@Url.Action("CreateModal")"
       data-modal-title="Crear WorkFlow">Crear</a>
    <a class="btn btn-secondary" href="@Url.Action("Index","TareasPrevias")">Tareas Previas</a>
    </p>

<div id="wf-grid-container" data-grid-url="@Url.Action("Grid")">
    @await Html.PartialAsync("_GridTable", Model)
</div>
